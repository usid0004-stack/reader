// Runtime configuration. Empty values = per-browser mode (no account, no sync).
// For cloud mode, run `npm run config` with SUPABASE_URL and SUPABASE_ANON_KEY
// in .env, or let the deploy build generate this file. The anon key is a
// public value; row-level security protects the data.
window.READER_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
